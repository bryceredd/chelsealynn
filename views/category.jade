extends layout 

block content
            
  .allImages
    each image in images
      .contentImage
        img.centerImage( src="/fit/1600/1200/#{image}" )

  .menu
    .allMenu
      each image in images
        .menuImage 
          img( src="/fit/2000/200/#{image}" )
